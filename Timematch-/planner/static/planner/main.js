document.addEventListener('DOMContentLoaded', () => {
    // Quick visual confirmation that app JavaScript is loaded.
    document.body.classList.add('js-ready');
    console.log('TimeMatch app JavaScript loaded');
});
